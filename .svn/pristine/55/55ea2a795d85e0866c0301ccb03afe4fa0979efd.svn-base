const Spinner = ({ design }) => {
  return (
    <div
      className={`${
        design ?? "h-6 w-6 "
      } animate-spin rounded-full border-[3px] border-current border-t-transparent text-primary-red-500`}
      role="status"
      aria-label="loading"
    >
      <span className="sr-only">Loading...</span>
    </div>
  );
};

export default Spinner;
